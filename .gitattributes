# При едновременни промени в историята на цените git да обединява
# редовете от двете страни, вместо да спира с конфликт.
# CSV-ът е append-only, затова обединяването е безопасно.
portfolio_history.csv merge=union
